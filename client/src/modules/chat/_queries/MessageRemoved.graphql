subscription messageRemoved($subscriptionInput: SubscriptionInput!) {
  messageRemoved(subscriptionInput: $subscriptionInput) {
    ... on MessageRemovedEvent {
      messageId
    }
    sender
    recipients
  }
}
